const caixaPrincipal = document.querySelector('.caixa-principal');
const caixaPergunta = document.querySelector(".caixa-pergunta");
const caixaAlternativa = document.querySelector(".caixa-alternaiva");
const caixaResultado = document.querySelector('.caixa-resutado');

const perguntas = [//abre lista de objetos (itens)
    {//abre o item
    enunciado:"pergunta 1"
    alternativas:[
        "alternativa 1"
        "alternativa 2"
    ]
    }
]
